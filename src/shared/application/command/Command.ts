/**
 * Mirror de Shared/Application/Command/Command.php
 * Marcador de tipo — cada Command concreto (CreateCourseCommand, etc.)
 * lo extiende para participar del mismo contrato que el backend.
 */
export abstract class Command {}
